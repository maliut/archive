require 'rufus-scheduler'

s = Rufus::Scheduler.singleton

s.every '1d' do
  # puts Time.now
  #time = Time.now
  #t_now = "#{time.year}-#{time.month}-#{time.day}"
  #Choice.where(state: 1).each do |choice|
  #  t_old = 
  #end
end